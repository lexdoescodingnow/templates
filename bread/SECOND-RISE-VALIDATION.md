# Second Rise validation

September 16, 2026.

- All 15 snippets have the correct type, a unique name, top placeholders, one hosted stylesheet link and complete `[dohtml]` wrappers.
- Posting HTML and CSS contain no comments, scripts, hidden editing notes or instruction blocks.
- CSS syntax parsed successfully. Theme selectors explicitly support Blue Hour light/dark modes and a system fallback.
- All 15 generated snippets exactly match their initial preview copy fields.
- All 45 zero/one/two-GIF editor states produced matching preview markup and copied code.
- All 15 editors converted bold, italic and underline BBCode, handled successive opening paragraph tags, escaped name fields and removed executable writing markup.
- All 15 reset controls restored the original snippet exactly.
- All five comms supported received, sent and alternating output.
- Nine combinations of preview mode and member palette updated the preview settings without exporting member-colour overrides.
- Category filtering and the successful clipboard path passed DOM integration checks.
- Every bud begins with fewer than 100 words and no writing container uses a fixed height or scrolling.

Visual browser rendering, image loading and an actual JCink submission remain unverified. The cloud browser refused to open the local HTML file under its URL security policy. DOM checks verify generated structure and editor behaviour, not painted layout or the forum's own parser and skin.
