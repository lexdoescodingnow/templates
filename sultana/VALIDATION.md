# Sultana validation

Checked on 8 September 2026.

## Completed

- Fifteen individually named snippets, five each of threads, comms and buds. No filename name conflicts with existing flavour collections.
- One complete `[dohtml]` block and one shared stylesheet link per snippet. `[url]`, `[name]` and `[text]` precede writing, and all editable content precedes decorative markup and the stylesheet link.
- No scripts, HTML comments, CSS comments, hidden editing tips or instruction blocks in posting snippets or the template stylesheet.
- Every untouched default generated by the preview matches its corresponding `.txt` file exactly.
- The stylesheet parses; all 266 style rules are scoped to `.sul`, with no invalid declarations reported by the CSS value validator. Inherited RGB colours, forward and reverse emphasis gradients, explicit forum theme selectors and the system fallback are present.
- DOM simulation checks passed for all design selections, three width controls, three theme modes, member palette changes, all fifteen gallery cards, category filters, open-paragraph comms, BBCode emphasis conversion, message direction, per-design edit retention, GIF removal and addition, safe body filtering and the clipboard fallback.
- Seven image-free defaults, seven single-GIF defaults and one paired-GIF default. Media-free layouts have explicit reflow rules; editor output removes empty media markup.
- Bud placeholder prose is 37 words. No writing container has a fixed height or clipping rule.
- Both supplied Tumblr URLs returned HTTP 200 and `image/gif`.

- The pinned jsDelivr stylesheet returned HTTP 200 with `text/css`; all 29,535 bytes matched the validated local stylesheet exactly.

## Verification limits

The Cloud browser rejected local preview navigation under its URL policy. No alternative browser route was used. Layouts were reviewed in source and editor interactions checked in a DOM simulation; pixel rendering, actual mobile appearance, browser font rendering and real clipboard permissions were not verified in this session.

The collection has not been pasted into a live JCink post. The forum must permit external stylesheets and provide the documented RGB variables and theme selector. Remote assets require internet access.
